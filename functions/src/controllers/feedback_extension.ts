// import * as admin from "firebase-admin";
export const getFeedbacks = (req: any, res: any, next: any) => {
  // admin.firestore().collection("feedback").where("")

};

export const createFeedback = (req: any, res: any, next: any) => {
  // admin.firestore().collection("feedback").where("")

};
